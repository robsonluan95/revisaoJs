
// =============================================
// 📘 Exercícios de JavaScript - Nível Básico
// ✏️ Operadores, Condicionais, Laços, Funções simples
// =============================================

// 1. Soma de dois números
function soma(a, b) {
  // return a + b;
}
console.log(soma(3, 5)); // 8

// 2. Verifica se número é par ou ímpar
function parOuImpar(num) {
  // return (num % 2 === 0) ? "par" : "ímpar";
}
console.log(parOuImpar(4)); // "par"

// 3. Retorna o maior de dois números
function maior(a, b) {
  // return a > b ? a : b;
}
console.log(maior(10, 20)); // 20

// 4. Imprimir números de 1 a N
function contarAte(n) {
  // for (let i = 1; i <= n; i++) console.log(i);
}
contarAte(5); // 1 2 3 4 5

// 5. Calcular fatorial
function fatorial(n) {
  // let resultado = 1;
  // for (let i = 1; i <= n; i++) resultado *= i;
  // return resultado;
}
console.log(fatorial(5)); // 120
